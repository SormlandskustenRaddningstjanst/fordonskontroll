# .env.example
# Copy to .env and replace placeholders with real values (DO NOT COMMIT .env)

# Client-side Supabase variables for Vite (must be prefixed with VITE_ to be exposed to the client)
VITE_SUPABASE_URL=https://rsmuycbunjxixlkglrwc.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY

# Optional: server-side secret (DO NOT expose to client). Only set this in GitHub Secrets for server jobs.
# SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY